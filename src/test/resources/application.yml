server:
  port: 80

  build.name: ABC-Tech-API
  build.version: 1.1

spring:
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
